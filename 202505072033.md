#编译原理
![[IMG_8779.png]]

![[IMG_8780.png]]

斜体符号，非终结符号，语法变量，终结符号串集合，产生式头；
非斜体的符号，终结符号，词法单元，词素；属性值是指向符号表的指针，符号表和文法没有关系。
构造的书写形式，产生式，语法描述，文法。

当考虑一个用一个生成式来描述一个语法时，其中非终结符号、产生式头的符号选择到底是 list 还是 statement，可以根据这个语法的英文或者中文语义来选择，比如一个形式如 9+3-5 的表达式，称之为"数位序列"，其英文则是 list。

为一颗指定的终结字符串构建一颗语法分析树的过程称为对该符号串进行语法分析。

---
大多数语句都是由一个关键字或者特殊字符开始的，赋值和函数调用除外。

---

我：S->S S + | S S * | a，这个上下文无关文法，请解读一下。


### GitHub Copilot

这个文法定义了一个简单的上下文无关文法，用来描述由 `a`、`+` 和 `*` 组成的表达式。以下是对文法的详细解读：

### 文法定义

S -> S S + | S S * | a

#### 1. **终结符**

- `a`：表示一个基本的输入符号（可以理解为一个操作数，比如变量或数字）。
- `+` 和 `*`：表示运算符。

#### 2. **非终结符**

- `S`：表示一个表达式。

#### 3. **推导规则**

- `S -> S S +`：表示两个表达式相加。
- `S -> S S *`：表示两个表达式相乘。
- `S -> a`：表示一个基本的操作数。

---

### 文法的含义

这个文法描述了一种表达式的结构，其中：

1. 表达式可以是一个单独的操作数 `a`。
2. 表达式可以是两个子表达式通过 `+` 或 `*` 组合而成。
3. 这种组合是递归的，因此可以构造出任意复杂的表达式。

---

### 结合性和优先级

1. **结合性**：
    
    - 由于文法是左递归的（`S -> S S +` 和 `S -> S S *`），这意味着运算符是**左结合**的。
    - 例如，表达式 `a a + a *` 会被解析为 `((a + a) * a)`。
2. **优先级**：
    
    - 文法中没有明确区分 `+` 和 `*` 的优先级，因此它们的优先级是相同的。
    - 如果需要区分优先级，可以通过引入新的非终结符（如 `T` 和 `F`）来调整文法。

---

### 推导示例

假设输入字符串是 `a a + a *`，我们可以用文法推导出这个字符串：

1. 初始状态：`S`
2. 应用规则 `S -> S S *`：
    
    S -> S S *
    
3. 对第一个 `S` 应用规则 `S -> S S +`：
    
    S -> (S S +) S *
    
4. 对两个最内层的 `S` 应用规则 `S -> a`：
    
    S -> ((a a +) a *)
    

最终，推导结果为 `((a + a) * a)`。

---

### 文法的语言

这个文法生成的语言是由 `a`、`+` 和 `*` 组成的后缀表达式（逆波兰表达式）。例如：

- `a` 是一个合法的表达式。
- `a a +` 表示两个操作数相加。
- `a a + a *` 表示 `(a + a) * a`。

---

### 改进文法（区分优先级）

如果需要区分 `+` 和 `*` 的优先级，可以引入新的非终结符。例如：

E -> E + T | T

T -> T * F | F

F -> a

在这个文法中：

- `+` 的优先级低于 `*`。
- `+` 和 `*` 都是左结合的。

---

### 总结

文法 `S -> S S + | S S * | a` 描述了一种生成后缀表达式的规则，其中：

1. `a` 是基本操作数。
2. `+` 和 `*` 是运算符，且具有相同优先级。
3. 运算符是左结合的。这种文法可以用来解析或生成后缀表达式，并通过扩展进一步支持优先级和结合性规则的调整。
---
6. 语义动作的示例
以下是一个完整的例子，展示如何通过语义动作计算算术表达式的值：

文法规则：
输入表达式：
3 + 4 * 5

语法树：
属性计算：
从叶子节点开始：

F.val = 3
F.val = 4
F.val = 5
计算 T 的值：

T.val = 4 * 5 = 20
计算 E 的值：

E.val = 3 + 20 = 23
最终结果为 23。

7. 总结
语义动作是文法规则中附加的操作，用于定义语法符号的语义。它们通过计算属性值、生成代码或执行语义检查，将语法结构与语义信息关联起来。语义动作在语法分析过程中动态执行，是编译器设计中语义处理的重要工具。

---
在构建语法树和解析表达式时，选择 `E -> E + T | E - T | T` 中的哪一个规则，取决于输入表达式和语法分析器的工作方式。以下是详细的解释：

---

### 1. **选择规则的依据**
在语法分析过程中，选择哪一条产生式规则（如 `E -> E + T` 或 `E -> T`）取决于：
1. **输入表达式**：
   - 语法分析器会根据输入的符号（如 `+` 或 `-`）来决定使用哪条规则。
2. **语法分析器的类型**：
   - **自顶向下分析**（如递归下降分析）：通过预测输入符号选择规则。
   - **自底向上分析**（如 LR 分析）：通过归约操作选择规则。

---

### 2. **自顶向下分析的选择**
在自顶向下分析中（如递归下降分析），语法分析器从起始符号 `E` 开始，尝试匹配输入表达式。选择规则的过程如下：

#### 示例：解析 `3 + 4`
文法：
```text
E -> E + T | E - T | T
T -> num
```

1. **从 `E` 开始**：
   - 当前输入是 `3 + 4`，第一个符号是 `3`。
   - 尝试匹配 `E -> E + T`：
     - 需要先匹配左侧的 `E`，但此时 `E` 需要展开为 `T`（因为 `3` 是一个数字）。
   - 尝试匹配 `E -> T`：
     - 成功匹配 `T -> num`，`num` 对应 `3`。

2. **匹配 `+`**：
   - 当前输入是 `+`，语法分析器选择 `E -> E + T`。

3. **匹配右侧的 `T`**：
   - 当前输入是 `4`，匹配 `T -> num`。

最终，语法树如下：
```
        E
       /|\
      E + T
      |
      T
      |
     num
```

#### 关键点：
- 自顶向下分析通过预测输入符号选择规则。
- 如果输入符号不匹配当前规则，分析器会回溯或尝试其他规则。

---

### 3. **自底向上分析的选择**
在自底向上分析中（如 LR 分析），语法分析器从输入符号开始，通过移入和归约操作构建语法树。选择规则的过程如下：

#### 示例：解析 `3 + 4`
文法：
```text
E -> E + T | E - T | T
T -> num
```

1. **移入 `3`**：
   - 当前输入是 `3`，将其移入栈中。
   - 栈内容：`[3]`

2. **归约 `T -> num`**：
   - 栈顶是 `3`，匹配规则 `T -> num`，归约为 `T`。
   - 栈内容：`[T]`

3. **移入 `+`**：
   - 当前输入是 `+`，将其移入栈中。
   - 栈内容：`[T, +]`

4. **移入 `4`**：
   - 当前输入是 `4`，将其移入栈中。
   - 栈内容：`[T, +, 4]`

5. **归约 `T -> num`**：
   - 栈顶是 `4`，匹配规则 `T -> num`，归约为 `T`。
   - 栈内容：`[T, +, T]`

6. **归约 `E -> E + T`**：
   - 栈顶是 `T + T`，匹配规则 `E -> E + T`，归约为 `E`。
   - 栈内容：`[E]`

最终，语法树如下：
```
        E
       /|\
      E + T
      |
      T
      |
     num
```

#### 关键点：
- 自底向上分析通过归约操作选择规则。
- 规则的选择由语法分析器的状态和栈内容决定。

---

### 4. **语法树构建中的问题**
对于语法树的构建来说，选择哪一条规则并不是问题，因为：
1. **语法树的结构由文法决定**：
   - 文法规则明确规定了语法树的形状。
   - 例如，`E -> E + T` 表示 `E` 是根节点，`E` 和 `T` 是子节点，`+` 是运算符。

2. **语法分析器自动选择规则**：
   - 无论是自顶向下分析还是自底向上分析，语法分析器都会根据输入符号和文法规则自动选择正确的规则。

---

### 5. **总结**
- 在解析表达式时，选择 `E -> E + T | E - T | T` 中的哪一条规则，取决于输入符号和语法分析器的工作方式。
  - 自顶向下分析通过预测输入符号选择规则。
  - 自底向上分析通过归约操作选择规则。
- 对于语法树的构建来说，规则的选择不是问题，因为文法规则已经明确了语法树的结构，语法分析器会自动完成规则的选择和应用。


---
语法分析等术语基本在和 gpt 讨论过程中了解了一些，不用再去重新理解书籍的描述，尝试，尝试再次直接开始正则表达式，但是正则表达式需要从章节开头的词法分析器的作用小节开始看起。
