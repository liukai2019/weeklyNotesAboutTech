#ebpf
User: 你是精通 Linux 内核、eBPF 和 libbpf 的高级工程师，同时也是导师。
我的背景：

工作语言是 C，维护 SIP 协议相关业务
有编译、链接、运行基础
学过 xv6、cs144
已经理解 eBPF 原理和发展，刚完成 sockfilter 示例
当前目标：研究下一个 libbpf 示例（先从 kprobe 开始）。
教学要求：
分阶段推进，不要一次讲完
多提问逼我思考，不要直接给答案
发现我理解偏差时要直接指出并纠正
重点讲为什么这样设计，不只讲代码在做什么
帮我建立整体模型（加载、附着、执行、事件回传、调试）
执行方式：
先给我一个 3 阶段学习路线图（每阶段目标、输入、输出）
每轮只给我一个小任务
我回答后你再纠偏并进入下一轮
任何结论都要结合实际代码路径解释，最好是先跑通一个整体再解释。
最终结果：让我可以独立读懂并改造该示例。

GitHub Copilot: 你这个目标我会按“导师模式”来带：先给你 3 阶段路线图，再只布置第 1 个小任务；同时我先快速读一遍 `kprobe` 示例代码，确保后面每个结论都能落到实际代码路径。接下来我会先做只读检查，然后给你第一轮任务和思考题。我先读取 kprobe 示例的关键源码文件，目标是把“加载→附着→执行→事件回传”这条链路定位到具体函数，确保给你的路线图是贴着代码走的。

Read [](file:///home/kk2018/ebpf/libbpf-bootstrap/examples/c)

Read [](file:///home/kk2018/ebpf/libbpf-bootstrap/examples/c/Makefile)

Read [](file:///home/kk2018/ebpf/libbpf-bootstrap/examples/c/kprobe.c)

Read [](file:///home/kk2018/ebpf/libbpf-bootstrap/examples/c/kprobe.bpf.c)

**三阶段路线图**
1. 阶段 1：先跑通整条链路（黑盒）
- 目标：把“加载→附着→触发→内核打印回传”一次跑通，建立最小心智模型。
- 输入：用户态入口 kprobe.c，BPF 程序 kprobe.bpf.c 和 kprobe.bpf.c。
- 输出：你能解释“为什么要先 open_and_load 再 attach”，并拿到一组真实的 ENTRY/EXIT 日志，知道它们分别对应哪个探针点。

2. 阶段 2：拆解 libbpf 设计（白盒）
- 目标：理解 skeleton 机制和 CO-RE 设计动机，知道 libbpf 在帮你自动化什么，边界在哪里。
- 输入：加载/附着调用点 kprobe.c kprobe.c，字段读取点 kprobe.bpf.c。
- 输出：你能画出“用户态对象-内核程序-attach link”的关系图，并能判断一个改动该在用户态做还是 BPF 侧做。

3. 阶段 3：可控改造与调试
- 目标：从 demo 走向可维护：加过滤、改事件通道、做故障定位。
- 输入：当前 printk 路径 kprobe.bpf.c kprobe.bpf.c。
- 输出：你能独立改造成“只追踪某进程/某目录”，并能用日志与错误码定位 attach 失败、无事件、字段读错这三类常见问题。

**第 1 轮小任务（只做这一件事）**
任务：先把 kprobe 示例完整跑通，并确认 ENTRY/EXIT 都出现。

1. 在目录 /home/kk2018/ebpf/libbpf-bootstrap/examples/c 编译并运行 kprobe。
2. 另一个终端读取 trace_pipe。
3. 触发一次 unlink 行为（例如创建后删除一个临时文件）。
4. 收集并贴回你看到的 2 类日志：
- KPROBE ENTRY pid = ..., filename = ...
- KPROBE EXIT: pid = ..., ret = ...

你先不要解释源码，我先问你 3 个思考题，你执行后只回答结论：
1. 为什么这里同时需要 kprobe 和 kretprobe，而不是只留一个？
2. 你观察到的 filename 和 ret，分别代表调用前状态还是调用后结果？
3. 如果只有 ENTRY 没有 EXIT，你第一反应会从“触发路径、附着状态、读取通道”哪一层先查？给出理由。

你回我日志和这 3 题答案后，我会直接纠偏并进入下一轮。