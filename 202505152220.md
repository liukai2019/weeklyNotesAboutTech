#软考 #多媒体基础 #网络部分 #数据库基础 

#### 并发控制

事务：把多种操作封装起来，因为很多操作有关联性，本身就是一个整体，如果不执行完那就会有问题。

比如转账，转出这个账户到转入到那个账户，必须一次性完成，原子操作。



**一致性类似能量守恒?**

隔离性：事务之间是独立进行的。

持续性：事务结束后影响是持续的。

事务在实际中的应用就是并发操作，并行执行。而并行操作会带来一些问题，比如更新丢失，因为两个线程，其中一个线程读取的是旧值，写入的是新值，所以只生效了一个线程的操作。不可重复读的后果，因为 T 2第 2 次读对象之前被 T 2 改变了，但 T 1 不知道，所以 T 1 认为两次读取的值相同但实际上不同。读"脏"数据：就是读到了临时值，T 1 进行了操作但产生的临时值还未写入物理内存，但这个临时值被 T 2 读取了，这个值不是 T 2 期望的，因为 T 1 可能会回滚操作，临时值是没有意义的。


加 X 锁，可以防止丢失更新，就是读写操作合并成原子操作，防止 T 1 和 T 2 同时写；
二级封锁协议，对于事务 T，读取 R 数据之前，先加 S 锁，S 锁是读锁（X 锁是写锁），别人还可以加 S 锁，但不能加 X 锁 (X 锁一旦加上不能加任何锁)，可以防止丢失、脏数据读。
三级封锁协议，在于事务 T，读取数据 R 之前，先加 S 锁直到事务结束才释放，可以防止丢失修改、脏数据读、数据的重复读。

两段锁协议，可串行化，可能发生死锁。

---
死锁有预防的方法，也可以在产生时进行甄别，在死锁发生后再解死锁。

----

数据库的完整性约束：
实体完整性约束、参照完整性、用户自定义完整性。

给数据表定义主键，主键的值不能为空??？

todo:
数据库大题；pv操作，磁盘计算；校验码计算；调度、页面计算；之间的todo：PV 操作、银行算法、页面逐出等视频再做题；
而数据结构、面向对象编程、流水线吞吐率、霍夫曼编码等就可以直接先做题![[IMG_8869.jpeg]]![[IMG_8868.jpeg]]