#linux内核设计与实现 

“精确重现一个 bug”这件事情为什么重要？

> 环形缓冲区的缺点是可能丢失消息，但是健壮性比较强

> Printfk 可以在任意非启动场景打印到输出设备

> 守护进程 syslogd klogd 为什么能操作环形缓冲区？

> 找出一个本不应该出现的数值

> CONFIG_KALLSYMS 选项如果开启，image 中就含有函数地址和函数符号的映射，这是否存在安全问题？

> 使用锁时睡眠一定会造成死锁？

各个调试选项，我应该记住 slab, high-memory, io mapping, sip-lock, stack-overflow, sleep-inside-spin lock, debugging…


> Bug_on, dump_stack (), panning. 这些函数可以帮助，好了，到这儿了的情况。


> 这本提到 gdb 没办法但不执行内核代码，不能加断电？不能修改内核数据？



```dataview
TABLE file.mtime AS "linux内核设计与实现的修改时间"
FROM #linux内核设计与实现
SORT file.mtime DESC
```

