#googletest 
  

Creating a new plan...

我将先把自动化任务拆成明确的 TODO 步骤以便跟踪，然后给出两种实现路线（轻量脚本与基于 AST 的稳定方案）、具体工具、示例生成策略和下一步可执行动作。

Added 1 todo

概览（一两句）

自动化有两条主路线：轻量“文本脚本”快速生成（可做函数包装类，但对被调用函数的签名推断受限）；稳健“AST/Clang”方案能正确解析签名、调用关系并生成可编译的 mocks/适配器，但环境和实现复杂度更高。下面按你的两个分布（生成单 header + 测试 cpp，或全部放到一个 cpp）分别给建议并附可行工具和实现步骤。

一、关键概念（快速参考）

要点 1：函数签名 + 参数/返回类型是生成 mock 的关键；文本替换对简单 C 函数有时可行，但对复杂 C++（模板、类成员、重载、命名空间、宏）不可靠。

要点 2：被测函数内部对其它函数的调用要“替换为 mock”，常用方法：a) 在编译时把调用改为通过可注入的函数指针/接口（需要源改写）；b) 在链接时用测试版对象/符号覆盖（link-time stub）；c) 运行期 patch（如 mockcpp）——风险与平台限制多。

要点 3：推荐先生成包装/测试类（把原函数放入类，供测试用），再逐步引入 mock 自动生成；这样增量可交付。

二、方案 A：快速脚本（文本操作），适合短期试验

目标：把源文件里的函数提取并包装成一个 class（例如 GTest_<OrigFile>），把原函数主体放到类方法里，导出一个小“桥接”原始 free-function（或替代原函数）来调用类实例的方法，测试文件单独写 gtest 用例。

优点：实现简单、不依赖 heavy tooling，快速产出。

缺点：无法正确解析复杂类型、宏展开、C++ 模板、类成员函数、宏生成的函数；对被调用函数不能自动生成准确 mock（只能生成空的声明/占位 stub）。

推荐用法（不破坏原始源代码）：

生成头 generated/<file>_gtest.h，包含：

class GTest_<file> { public: /* copied function code as methods */ };

对每个 free function 生成 static 方法或成员方法，并在 header 末尾生成一个 inline wrapper free-function 调用全局可访问的 GTest_<file>::instance().method(...)（或提供 macro 开关让测试编译时用 wrapper 替换原定义）。

在测试 cpp 中，包含该生成头并编写 TEST()，通过 setting up instance 状态模拟输入，或者通过手工 stub 装入被依赖函数（若这些被依赖函数声明简单可手写 stub）。

工具/实现：一句 Python 脚本（re-based）。示例伪代码片段：

使用 regex 找到 return_type name(args) { body }（仅限简单函数）；把 body 塞进 class method，导出 wrapper extern "C" return_type name(args) { return GTest_file::instance().name(args); }（注意链接问题）。

实施步骤：

选一个简单 C 源做 POC。

写 Python 脚本：读取 source, 用 regex 提取签名与 body, 生成 header。

调整 tests 的 include 顺序（先 include generated header）。

手工为关键的 callees 写简单 stub declarations（或在 tests/include 提供 headers）。

何时合适：函数简单、项目主要是 C、你能接受手工补充一些 callees。

三、方案 B（推荐长期）：基于 Clang/AST 的生成器（准确、可扩展）

目标：用 libclang / Clang LibTooling / clang-query / CastXML 等解析源码，准确提取函数声明、返回/参数类型、调用图、用到的宏/typedefs，然后生成：

a) 一个 header 包含包装类（把原函数重构为类方法或生成可注入适配器），以及对被调用函数的声明或 mock 接口。

b) 可选：自动生成 GoogleMock 接口或 mock implementations（如果被调用函数是抽象化的接口或函数指针易替换）。

优点：解析准确，能处理复杂 C/C++（命名空间、overload、templates—模板处理仍有限但可识别）、能生成准确签名用于 mock。支持进一步源码改写（Clang Rewriter/LibTooling）以把调用替换为注入点。

缺点：依赖 clang/libclang 环境，工程集成稍复杂（Windows 下需注意 libclang version），实现需要较多时间。

可用工具：

libclang (clang.cindex) — Python bindings: 解析 AST，提取 FunctionDecl、CallExpr、Type。

Clang LibTooling / clang-tidy / clang-query — 更强的 C++ 源改写能力（C++ 特性支持更完整）。

castxml + pygccxml — 若你只需要声明级信息（C and C++ symbols），这套工具能把 header->XML再用 Python 读出（对模板支持有限但可用）。

srcML — 将源码转成 XML，便于查询；适合源重写。

tree-sitter — 解析 C/C++，速度快，可用于编辑器集成，但语义分析（类型）不如 clang。

生成策略与替换方式（几种可选）：

Adapter pattern (source rewrite): 使用 Clang Rewriter，把每个对外调用 site（被测函数中）改为 callable_ptr(...)，并在生成类中注入 std::function 或函数指针成员，测试时替换为 lambda/mocks. 这需要編集源文件或编译时包含替代头。

Wrapper + link-time override: 生成一个 generated_stub.c 里定义同名 functions (weak symbols) to override original during test linking. Works for linkable symbols and when linking test with generated object first. Less intrusive, but may fail for static functions or inlined functions.

Class extraction: Move original function body into a class method (via generated code), and modify original function to call that method—requires source rewrite but keeps semantics.

Runtime patch: Use mockcpp-like runtime patching—generate hook code automatically (more complex, platform dependent).

实施步骤（recommended):

Set up a Python prototype using clang.cindex to parse a single source file. Extract all FunctionDecl nodes and for each record signature (return type, param types, qualifiers) and the list of called functions (CallExpr).

Generate header with:

Class GTest_<file> with methods for each extracted function (copy body via source ranges).

For each callee encountered, generate a forward declaration with exact signature (so test compilation can compile and link).

Optionally generate gmock interfaces if callees are C++ virtual methods or if you detect they belong to an interface class.

Add a CMake custom command that runs the generator before compiling tests; generated files go into generated/ tracked by CMake as add_custom_command(OUTPUT ...) and add_custom_target(gen_tests ...).

Iterate to handle macros and includes: the generator must output #include of original headers or inject necessary typedefs; or tests must compile with same include path set (you already collected -I -D).

注意点：

For C code, libclang approach is straightforward. For advanced C++ features (templates/macros), you may need to compile the parsed translation unit with same -I -D flags (clang allows passing args), so libclang resolves types.

Inline/static functions: static/internal linkage functions cannot be overridden at link-time; prefer source-adapter approach.

If the function bodies heavily use macros or preprocessor, you must run clang parse with same preprocessor flags.

四、对两种文件分布（single header + separate test cpp VS all-in-one cpp）

Option 1 — Generated header(s) + test .cpp separate (recommended)

好处：清晰分离生产代码与测试生成代码，便于版本控制、审查；生成 header 作为“testing harness”容易被多个 test files 复用；CMake 集成更清晰（生成目标 / outputs）。

实现要点：生成的 header 必须 be self-contained or include the original headers (and you must ensure include paths and -D are applied in test compile). Use add_custom_command to generate before test build.

Mock 自动化：AST 方案能为每个 callee生成 accurate forward declarations; then you can generate gmock mock classes for C++ interfaces, or for plain C functions generate weak-linkable stubs or use adapter pattern to inject function pointers.

Option 2 — Everything in one .cpp (original mock + function + tests in same file)

好处：最简单把所有东西放一起，测试编译独立且可快速运行；对于简单 projects this is fastest to prototype.

缺点：less modular，生成文件可能变大，难以在多个测试间复用。

Mock 自动化：同上，但 easier to inject stubs locally because you can define static overrides in same translation unit.

五、实现示例（可运行的最小 POC）

轻量文本脚本 POC（Python pseudo; 仅示意）

libclang-based approach (outline):

Install clang and python-clang (pip install clang or use system libclang).

Python code: create Index.create(), parse TU with args args=['-I', '...','-D','...'], walk AST nodes, extract CursorKind.FUNCTION_DECL, use get_extent() to fetch source text for body, and produce generated header/class.

This approach gives accurate parameter types and return types for generating forward declarations and mocks.

六、集成到 CMake（建议）

Add a custom target to generate files:

Important: pass the same compile flags to the generator (include dirs, defines) so clang can parse correctly.

七、风险与兼容性说明（务必阅读）

自动生成修改源调用必然有风险（尤其当代码中有宏、复杂模板、平台特定代码）。

先做生成“测试 harness header + forward declarations”并手动完善 mock 比直接改写源更安全。

对于 C++ 的类成员、模板、重载及内联、static 需要特殊处理 —— 推荐先在小范围验证再扩大到全仓。

八、建议的可交付路线（最小到可扩展）

立刻做：用轻量脚本在一个简单 C 文件上做 POC（Option A），验证流程与 CMake 集成。——可在几小时内完成。

中期（推荐）：实现基于 libclang 的生成器，能正确提取签名并生成 forward declarations 与 class wrappers；把生成器集成到 CMake（几天工作量，取决复杂度）。

长期：若需要把 callees 自动 mock（完全自动），基于 AST 的源码改写（Clang Rewriter / LibTooling）或生成 link-time stubs 并控制链接顺序是可行方案（实现难度最大）。

九、我可以为你做的下一步（选一）

我做一个“快速 POC”脚本（Python）针对你仓库里某个具体 C 文件，生成 class header 并演示如何写对应的 test .cpp，然后运行构建（最小风险、快速反馈）。

我搭一个 libclang 原型：解析一个复杂的 moduleA 源文件，生成 header + callees 的 forward declarations（并展示如何生成 gmock skeleton），并把生成器作为 CMake pre-step集成。

我先为你写一份更详细的实现计划（任务分解、所需依赖安装步骤、样例输入/输出）让你评估实施成本。

告诉我你想要哪一种（我推荐先做 1 快速 POC，然后升级到 2），并告诉我：

源文件是 C 还是 C++？（若 C++，是否有 heavy template usage）

你愿不愿意在机器上安装 clang / libclang（若要做 AST 路线必须）？

指定一个样例源文件路径（例如 src/moduleA/folder1/subFolder1/foo.c），我就可以开始做 POC 并把生成的文件和 CMake 变更交付给你。