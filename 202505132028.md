#开发模型 #软考 #软件工程
指导开发的思想、体系。

---

瀑布模型 SDLC 是结构化开发的模型
软件计划、需求分析、软件设计、程序编码、软件测试、运行维护。强调评审和标准化、回溯，但缺陷在于需求阶段难以把控，软件需求在项目初期都是不明确的，瀑布模型几乎都会失败，考题：瀑布模型适用于需求明确、二次开发。

一组有关联的模型：
演化模型、螺旋模型、原型模型、增量模型、快速原型模型，原型模型定位于需求不明确的情况，初期很难表达需求因为用户自己也不知道需要什么需求，原型法强调项目初期构建一个简易系统，可以是一个界面、初步的系统让用户看一看用一用，**原型法只用于需求初期阶段**。原型模型可以发展为演化模型、螺旋模型，增量模型是在瀑布模型的核心模块基础上增加。

螺旋模型融合了多个模型，考题：一个需求不明确的项目应该用原型模型而不是优先选螺旋模型；引入了**风险分析**，是螺旋模型的最显著特征。

V 模型以其形状得名，和瀑布模型相近，但是更强调测试，每个测试阶段和设计等阶段一一对应，想要提前发现问题。

---

喷泉模型是面向对象的模型，就有迭代、无间隙的特点；

---

RAD 是快速开发模型，CBSD 是构建化开发模型。RAD 就是有很多现有的标准的模块、构件库可用。特点是**能够快速化构建系统**。

CBSD 能够得到广泛认可是因为很大提高了复用性，降低成本，增加可靠性。构建库能够复用，可靠性经过了很长时间的验证。
需求分析和定义、软件架构设计、构建库的建立（看看以前的构件库）、应用软件构件，CORBA, COM/DCOM/COM+, EJB 三大构件标准。

---
统一过程可以用在大型项目。
用例驱动，通过需求分析挖掘出用例，用例又称为开发的示例；
以架构为中心，强调把架构设计好；
迭代与增量，形成一个环状，一个循环完成有一个增量的完成，案例分析和选择题都可以用到，UP 就是其缩写，RUP 是另一个缩写。统一过程的开发分为四个块：初始、细化、构建、交付。

初始：确定范围和边界、识别关键用例、候选架构