#软考 #数据库基础 

--------------
分隔符

----
第一范式：在关系式 R 中，当且仅当所有域只包含原子值，每个分量都是不可再分的数据项，则称 R 是第一范式。如图 10 所示：
![[IMG_8859 1.jpeg]]

第二范式：主键有时是组合键也就是多列，比如学生选课的成绩，只有学生号和课程号才能唯一确认成绩。消除了部分依赖才是满足了第二范式。

数据冗余：而课程号可以单一决定学分，学分属性部分依赖主键，这带来了数据冗余，从一行行地看，有多行课程号需要存储学分，这多行可以合并成一行。

插入异常：只插入 C 08 课程号-6 分，是不能完成的，因为还没有学生选 C 08。只有学生号+课程号才是主键，才能插入。

删除异常：比如这届学生毕业了，如果删除学生属性，那么这个表格的学分等信息也删除了。解决方法：把 CNO 和 CREDIT 提取出来做新的表格。


主属性：这一列属于候选键的一部分。其他的列就是非主属性。


有点混乱：
我总结一下，第二范式是消除了非主属性对候选键的部分依赖，第三范式是消除了非主属性对候选键的传递依赖（传递依赖涉及两个非主属性和一个主键，该主键有这样的性质：非主属性对主键是完全依赖的，非主属性不能单独依赖其中一个主属性）。这里的键又涉及根据描述画图了，或者在题目给出图的基础上，找出入度为 0 或者组合各节点以找出主键。

例题：如果一个图，或者一个描述中，没有找到非主属性，那么直接满足第三范式，第 1、2、3 范式都是对非主属性的约束。

BC 范式的举例：
文字描述：关系模式 STJ (S, T, J) 中，S 表示学生，T 表示老师，J 表示课程，每一老师只教一门课程，每门课程有若干老师，某一学生选定某门课程，就对应一个固定老师，而下图可能是题目中给出的，也可能是解题者自己画出的：#
![[Pasted image 20250515200908.png]]
SJ->T，左边的 S 和 J 都是主键。

入度为 0 的节点只有 S，但 S 不能作为候选键，因为单独 S 到不了 T，所以 SJ 是一个候选键，而 ST 也是一个候选键，因为 T 可以确定 J，所以 ST 出发已经能遍历完所有节点。

ST 和 SJ 都是主选键，S、T、J 都是主属性，没有非主属性，所以满足第 3 范式。

所有函数依赖写出来，依赖的左边部分必须是候选键。
SJ->T，T->J，所以这个图不是 BC 范式。J 不需要 ST 才能确认，只有 T 就能确认 J 缺少了 S 所以不是 BC 范式。

---
规范化理论-例题![[IMG_8861.jpeg]]
![[pic(5).jpg]]

消除部分依赖：必须是主键是多个属性的组合键，而这里部门号是可以作为主键的，所以不存在部分依赖；
所以第一空只有 C。传递依赖的分析不用继续分析了。

第二空，表 4 是职工和部门以及销售额的表格，职工号和部门的关系是表 1 2 3 没有提到的，所以要在表 1 或者表 2 3 中增加一列，而在表 3 中增加一列部门号是可以结合表 1 2 3 得到表 4 的，但是有个疑问是这里似乎会增加冗余，因为多个职工属于一个部门，所以在表 3 中增加部门号，那么有多行的部门号是相同的? 不管如何, 第 2 空应该填写 D。

第三空，有哪些内容是表 1 2 3 是无法表达的，C 和 D 的第二个属性部门号和第 2 空新增的列冲突，因为第 2 空的增加使得职工号和部门号一一对应，而 B 选项商品号和商品名称是冗余的，所以选择 A。

---
规范化理论中模式分解。

范式级别不够时，将模式进行拆分?
拆分时需要考虑一些因素，拆分过程中有一些机制、原则？

分解的两个要求：
1）保持函数依赖的分解：分解之前有哪些函数依赖，分解之后依然存在这些函数依赖。
比如有一个关系模式，有 A BC 三个属性，A->B，B->C，分成两个关系模式 R 1 (A, B)，R 2 (B, C)，如何分解的？虽然这样的分解保持了函数依赖，因为 A->B 的函数依赖被 R 1 保存了下来，另一个同理。如果再拆分一个出来，R 3 (A, C), 那么这种拆分就没有保持函数依赖。

哦我知道了，分解就是分解表格.. **表格是可以分解的**

对于冗余性质的函数依赖，比如 A->B, A->C, B->C，因为 A->B->C 能够唯一确定，但 A 又能唯一确认 C，所以有冗余依赖，所以**这个地方是 C 部分依赖主键**？总之，这里 R 1 R 2 R 3 的拆分保持了函数依赖。

2）无损分解。进行压缩时也会用到有损无损的概念。
有损就是不能还原。

**还原就是现在的表格通过连接、投影等运算能够换遇到原来的关系模式（原来的表格）。**
![[IMG_8863.jpeg]]
![[pic(7).jpg]]



以上图是无损分解。

列表法、公式法是书籍上讲解的方法。
表格法：a 代表当前关系模式会真正拥有当前属性，比如a 1 是第一行第一列，拆分后的成绩关系模式拥有学号这个字段，b 12 ，b 表示当前关系模式没有包含这一列的属性，这一列在当前构造出来的表格中是第 2 列。这就是初始表。
![[IMG_8867 1.jpeg]]
再给出相关函数依赖，如图所示：，那么就可以根据函数依赖修改初始表。最后有一行全是a 那就是无损分解。

第二种方法是公式，只适用于 1 分为 2，R={R 1, R 2}，如果拆分成三个就不适用，拆分成两个很高效。如图的例题所示：![[IMG_8866.jpeg]]
