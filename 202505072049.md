> Init 函数通常会注册资源、初始化硬件、分配数据结构等


> 如果文件被编译到内核映像中那么 exit 函数永远不会调用，因为 kerne 不会退出。

> [!question] 非 gpl 模块不能调用 gpl_only 符号？


my_module/
├── Makefile          # 主Makefile
├── Kconfig           # 配置选项(如果集成到内核)
├── src/
│   ├── main.c        # 模块主代码
│   └── helper.c      # 辅助功能
├── include/
│   └── my_module.h   # 模块头文件
├── tests/            # 测试代码
├── docs/             # 文档
└── scripts/          # 辅助脚本

原来这是构建模块代码的标准文件目录。


---
导出符号表和统一设备模型是接下来需要搞清楚的两个术语，以及其解决了什么问题。

在这两个概念前都介绍的是一些零散的设备相关代码，模块参数、加载模块的命令，依赖性管理以及安装等命令行命令。


还有一个 sysfs 的虚拟文件系统。

好了就剩下调试章节了。

