#RSASSA-PSS


#### 数论回顾
问题：研究不同类型的自然数之间的关系
$$
\gcd(a, b) =
\begin{cases}
a, & \text{if } b = 0 \\
\gcd(b,\, a \bmod b), & \text{otherwise}
\end{cases}
$$

$$
\gcd(a,b) =
\begin{cases}
a, & \text{if } b = 0 \\
gcd(b, \, a\bmod b), & \text {otherwise}
\end{cases}
$$  $r_-1=a$, $r_0 = b$
$r_{i-1} = q_{i+1} * r_i +r_{i+1}$

$gcd(a, b) = 1$ 那么 a 和 b 互素，a 和 b 互素且 a 和 b 较小时很容易从直觉上觉察但是 a 和 b 很大时，a 是否与 b 互素则需要借助 gcd 符号表示并使用欧几里得算法也就是带余除法来获得。欧几里得算法本身是为了求取最大公因数，如果算出来最大公因数就是 1，那么可以说 a 和 b 本身互素，因为这就是互素的定义，最大公因数是 1 那么两个数互素。

带余除法

整除 n 的数称为 n 的因数


##### 线性方程与最大公因数
$ax+by$ 的每个数都被 $gcd(a,b)$ 整除，因为 $q_1gcd(a, b)x+q_2gcd(a,b)y=gcd(a,b)(q_1x+q_2y)$

