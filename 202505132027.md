#UML #软考 

（1）例题 1
类图和状态图和面向对象结合：
浏览题干时根据问题详细看题目，比如填写 A~F 的类名，和类图中的多重度，多重度就是描述中的数量关系。

聚合是空心菱形；组合是实心菱形；泛化是空心三角形，而三角形所在的矩形框是父类；聚合表示多个组成一个；组合是指多个相同属性但不同的实体组成。

多重度就是表示 1 对多还是什么数量关系：
歌手和乐队的数量关系：
表达一个乐队有两个或者以上的歌手的符号为 2..*
一个歌手可以不属于任何乐队，也可以属于一个或者多个乐队，符号为 0..*
歌曲和音轨的关系：一个歌曲可能出现在多个音轨，符号为 1..*
一个音轨只能出现一首歌曲，符号为 0.. 1

问题是连接符号有两头或者两个空，应该填写 1..*  还是 0.. 1 ，分析：靠近谁，这个数量描述的就是谁，远端数量就是 1.

一张唱片可以有多个音轨，所以靠近音轨的矩形框的多重度应该填写：1..*
一个音轨只能有一个唱片，所以靠近唱片的矩形框的多重度应该填写：1

类图中的类可以关联自己，所以可能有两个相同的类矩形框。

事件序列？
状态内部可能还有状态，而不是常规的有穷状态机的图。

一道题可能出现 2 种或者 3 种图，所以要背诵记忆这些图。

（2）例题 2
UML，设计模式，面向对象。

状态图，状态变迁的条件填空题，类名；
设计模式：状态图，属性：
Travel 方法应该具有调整飞行里程数的的功能；
必须具有.. 属性？

---
回过头来再看总体的，面向对象技术

1 基本概念
对象、类（实体类、边界类、控制类）、抽象、封装、继承和泛化、多态、接口、消息、组件、模式和复用。
其中多态、接口等会考填空题，模式和复用比如架构、设计模式需要关注：

2 设计原则
单一职责原则：设计目的要单一；
对扩展开放，对修改封闭。开放-封闭原则。
LISKOVD 原则，子类可以替换父类，。因为有继承关系，所以考虑对子类的方法进行重载时，需要考虑好不要影响这个元组。
依赖倒置原则：要依赖于抽象而不是具体实现；针对接口编程而不是针对实现编程。
接口隔离原则：使用多个专门的接口而不是单一的总接口。这就是为什么 IMS 代码和外部模块之间有多个接口。
组合重用原则：要尽量使用组合，而不是继承关系达到重用目的。因为父类一旦变化，那么子类也就变化了。这就是避免耦合，不要依赖。
迪米特 demeter 原则，最少知识法则：一个对象应当对其他对象有尽可能少的了解。比如，不要传大结构体层层地传。

3 如何区分设计模式、架构模式和惯用法
设计模式和架构模式的区分是看从全局还是局部触发；
惯用法和设计模式的区别是惯用法要和具体的编程语言结合，比如 c++的引用计数就是惯用法。

设计模式分为创建型模式、结构型模式、行为型模式，其中创建型模式中有名的就是工厂模式，工厂模式是为了复用业务逻辑而只生产不同的对象来的，这里可以借鉴 CSM 模块的 call、service 等，其实 ISI 的函数可以尽可能复用而不要产生新的逻辑。

结构型模式处理的是对象之间、类之间的关系，最典型的是适配器模式，adapter，比如充电器就是电源适配器，手机和插座之间的适配；tcp 和 ip 等包的嵌套封装也可以用适配器来实现。

行为模式处理的是对象的职责、对象之间的互动，最典型的就是适配器：这里需要记忆：
职责链、命令、解释器、迭代器、中介者、备忘录、观察者、状态、策略、模板方法、访问者；

结构型模式：适配器、桥接、组合、装饰、外观、享元、代理模式。

创建型模式：工厂方法、抽象工厂、原型、单例、构建器。

既可以是对象也可以是类模式：工厂方法、适配器、解释器、模板方法。

---
创建型模式：

构建器模式：一个复杂对象不去创建相应对象，而是分开创建较为简单的类，由不同类实例组合成这个复杂对象。

工厂方法模式：定义一个创建对象的接口，但由子类决定需要实例化哪一个类。工厂方法使得子类实例化的过程推迟，具体的就是运行时，由子类决定创建哪一个类的对象。这样可以复用业务逻辑，但对象不同。

原型模式是为了避免 new 一个对象的开销而只拷贝其内存区域，开销小、效率高。

单例模式是只有一个对象，比如浏览器只有一个主窗口。

抽象工厂模式无需指定具体的类，而只需要指定系列？

---
结构型模式

适配器模式：转换不同的接口成统一的接口，比如adapter 的模板。
桥接模式：类树太大了，拆解成几棵树，树与树之间是桥接的关系以达到原来树相关的效果。
装饰模型：附加职责。而不是重新派生，消耗高。
外观模式：统一一组接口的出口
享元、代理


---
行为型模式
职责链模式：减少发送者和接收者之间的耦合；接收者之间链接起来，在链中传递请求。比如 call list 中的 call 的选择，以及视频中的举例，不同职位人员的财务审批权限，会传递职责。
命令模式: 将请求封装为一个对象而不简单地是一个消息，有了撤销功能等。
解释器模式：根据文法解释语言中的句子，比如虚拟机。
迭代器模式: 能够顺序访问聚合对象中的各个元素而不用暴露内部表示。迭代器。
中介模式：为了降低耦合程度，通过中介通信。
备忘录模式：快照一个对象，存储此时这个对象的信息以便恢复等操作。
观察者模式：一对多的关系，subscribe，一个有变化、对应的多个都会变化。
状态模式：允许一个对象在其内部状态变化时改变它的行为。状态变成类。比如 processEvent 根据. C 不同而有不同的行为；比如视频中的举例：会员等级
策略模式：一个问题处理时可以切换不同方案。
