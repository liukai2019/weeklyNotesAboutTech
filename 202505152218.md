#软考 #多媒体基础 #网络部分 #数据库基础

Next 步骤：剩余的剩余压缩等概念和计算，网络部分。
数据库。
PV 操作、银行算法、页面逐出等视频再做题；
而数据结构、面向对象编程、流水线吞吐率、霍夫曼编码等就可以直接先做题

---
数据压缩：空间冗余、时间冗余（一帧和下一帧很多相同）、视觉冗余（人的视觉甄别不到一些颜色）、信息熵冗余、结构冗余（很多块都是相同结构，比如地砖）、知识冗余。

有损压缩编码是无法还原的，因为已经有信息丢失了，**jpeg 是有损压缩**
无损压缩，**rar 是无损压缩**

无损编码：霍夫曼编码，把使用频度高的编码编得短一些，把使用频度低得编码编得长一些；

---
网络部分还是跳过吧，直接数据库。

---
三层视图、三层模式，两层映射。
用户视图 DBA 视图内部视图

外模式是为了形成外部的视图不随着概念模式数据变化而变化, 不必修改用户程序；
内模式在做存储的事情，也就是物理级数据库，也就是文件；
概念模式就是俗称的表格。

示例：登录时不需要把整个表格拿出来，只需要其中一些列拿出来形成登录视图。

表和视图的映射就是外模式-概念模式的映射。

存储结构改变不需要改变表格，这就是概念模式-内模式映射。

---
数据库的设计介绍

需求分析->概念结构设计->逻辑结构设计->物理设计

概念结构设计的主要表达形式是 ER 模型，跟数据库管理系统没有关系的模型，可以转为 access、oracle 等数据库，是一种数据的表达，有哪些数据实体，数据实体的属性和实体之间的关系等。

ER 模型转为关系模式，就是转化为文字表达、一个个表的情况，转化过程中就涉及**规范化理论**。关系模式是逻辑结构的产出物。

数据库管理系统的特性逻辑结构设计->物理设计中引入的，也就是 DBMS；而概念结构设计->逻辑结构设计过程中只有用户数据模型，这是和 DBMS 无关的概念模型。

需求分析是看用户对存储有什么要求。

---

ER 模型

方框表示实体，椭圆表示属性，菱形表示联系。

E-R 模型先局部再集成，集成方法有两个：一次性集成、逐步集成；集成过程的冲突可能有属性、命名、结构冲突（比如老师这个字符串，在一个局部模型中是表格，另一个局部模型中是一列，抽象级别不一样）。

ER 模型转成关系模式的原则：一条实体转成一个关系模式，联系类型有 3 种，1 : 1 联系，1: n 联系，n: n 联系。
1: n 的解释示例：一个部门对应 n 个员工，两个实体之间的关系转为关系模式可以放在 n 个员工的实体处。第三种情况多对多，n: n，学生和课程之间就是多对多的关系，学生这个实体必须转成 n，课程这个实体也要用 n 这个数量来描述，而两者之间的关系由于学生可以选择多门课程，每门课程也可以有多个学生，所以两者关系也必须要用一个 n 来描述，用术语描述**学生和课程之间的多对多联系最少可以转换为三个关系模式**。

每个实体必须转换成一个关系模式，比如三个实体之间是多对多的关系，需要 4 个关系模式。

---
关系代数
给出关系代数表达式，找出等价的关系表达式；
给出业务场景，要完成什么样的只能，让写出关系表达式。

基本的运算：
并、交、差，笛卡尔积、投影、选择、联接。

![[IMG_8852.jpeg]]
![[pic(1).jpg]]

笛卡尔积：
属性数（列）是原来的表格的属性之和，记录数（行）是原来两个表格的记录数之积。笛卡尔积不会将相同列去掉。
![[IMG_8853.jpeg]]

![[pic(3).jpg]]

投影是选择列的操作，如上图所示，也可能是 $\pi_{1,2}(S1)$
选择是选择行的操作，如上图所示, 也可能是 $\delta_1(S1)$

图 3
连接操作只保留一个，没有写条件就是自然连接，两个关系中相同的字段做等值连接，如图 3 所示。而连接则会去掉相同列的连接。

---
规范化理论，而函数依赖是其基础部分, 如下图 4 所示。

举例：学号能够决定姓名，一个数据表中姓名却不能决定学号。

理论 1：部分函数依赖
            ![[Drawing 2025-05-15 10.44.25.excalidraw]]

组件的一部分可以确定某一个属性，比如 (学号，课程号) 可以确定姓名，但是其实学号就可以确定姓名。

理论 2：传递函数依赖
![[Pasted image 20250515111159.png]]
如果 A 和 B 等价，则不存在依赖的传递。


---
规范化理论的价值和用途

如图 5 所示，存在数据冗余，冗余就可能更新异常（有几个entry 遗漏了），插入异常（两个属性的组合作为关键字、组件，两个属性中的一个还没有明确时，就不能插入，这就是插入异常），删除异常（删完了才发现有不应该删除的）

规范化理论就是解决以上异常的方法。

反过来，有些需求需要非规范化数据，一位规范化理论存在一些问题。这就是矛盾，比如安全和性能的矛盾。

| SNO | SName | DNO |
| --- | ----- | --- |
|     |       |     |

---
规范化理论-键

如图 6
超键：唯一标识元组, 可以是单个属性，也可以是属性的组合。
而候选键是考题中重要的概念。
而唯一标识元组的在教科书上说就是候选键，候选键和超键区别是：超键消除多余属性后就是候选键。

学号，姓名，性别。

学号和姓名能够确认性别，学号也能唯一确认性别，学号和姓名就是超键，这具有冗余，而学号就是候选键。

候选键和主键的区别：候选键可以有多个，而主键只有一个。

外键：别的关系的主键。用于关联表和其他表。

部门号对于员工是一个外键，部门号是用于标识部门表的而不是标识员工表。


求候选键，有时考题比较复杂，求候选键不能立刻看出，一些方法如图 7 所示：
![[IMG_8856.jpeg]]
例题如图 8 所示：
![[IMG_8858.jpeg]]

（1）将关系模式的函数依赖关系用"有向图"的方式表示。注意 A->E，B->E, D->E 和 ABD->E 是不同的，ABD->E 要汇聚成一个节点到 E，只有一个箭头。
（2）找出入度为 0 的属性，将图上的节点遍历一遍，如果能够遍历一遍那么该节点或者该属性就是候选键。
（3）如果任意一个节点都不能遍历全图，那么要找到组合键才能遍历全图。
（4）没有入度为 0 的属性或者节点，那么要找中间节点，既有入度也有出度。候选关键字是可以有多个的。


---
规范化理论-范式
函数依赖、候选键的求解是范式的基础
范式的基本结构和形态是：第一、第二、第三、第四范式，BCNF。

规范化越高范式越高，但数据的粒度越小，范式要提高级别，那么方法就是数据表的拆分，范式越高数据表拆分越细，数据一步步拆分会带来性能的问题，所以往往采取折中的程度,一般第三范式就足够了。

范式级别最低：第一范式，要达到第二范式必须先达到第一范式，1 NF 也就是第一范式就是属性值都是原子不可分的。如图 9 所示。
![[IMG_8859.jpeg]]
规范化理论（Normalization Theory）是关系数据库设计中的一套理论和方法，用于组织数据结构，减少冗余，提高数据一致性。规范化过程将数据表分解为更小、更结构化的表，同时保持数据的完整性。规范化的每一个阶段称为一个“范式”（Normal Form, NF）。

下面介绍常见的范式及相关术语：

---

### 1. 第一范式（1NF）

**定义**：表中的每个字段只能存储单一值，不能有重复的列，也不能有集合、数组等复合属性。

- **非主属性**：不是候选键的属性。
- **候选键**：能唯一标识一行记录的最小属性集。例如，学号或身份证号。
- **函数依赖**：如果通过属性A的值能唯一确定属性B的值，则称B函数依赖于A，记作 A → B。

---

### 2. 第二范式（2NF）

**定义**：在满足1NF的基础上，表中的每个非主属性都完全依赖于候选键，而不能只依赖于候选键的一部分（消除部分依赖）。

- **完全依赖**：非主属性依赖于整个候选键，而不是其中的一部分。
- **部分依赖**：非主属性只依赖于候选键的一部分（只在复合主键时出现）。

---

### 3. 第三范式（3NF）

**定义**：在满足2NF的基础上，表中的每个非主属性都不依赖于其他非主属性（消除传递依赖）。

- **传递依赖**：非主属性依赖于另一个非主属性，而这个非主属性又依赖于候选键。例如，A → B，B → C，则C传递依赖于A。

---

### 4. BCNF（巴斯-科得范式）

**定义**：在满足3NF的基础上，任何非平凡的函数依赖X → Y中，X都必须是候选键。

---

### 术语总结

- **非主属性**：不是候选键的属性。
- **函数依赖**：属性集A的值决定属性集B的值，记作A → B。
- **候选键**：能唯一标识一行记录的最小属性集。

---

**规范化的目标**：  
通过分解数据表，消除冗余和异常，确保数据结构合理，提高数据一致性和可维护性。

