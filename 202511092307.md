#googletest
"- 如果你要 mock 的函数是“从另一个 DLL 导入的函数”（即被调用方为独立的 DLL，调用通过导入表/Import Address Table），那最简单可行的方式是：让测试加载一个同名的 mock DLL（或替换 import lib），测试运行时就会调用 mock 实现。  
- 如果被调函数和调用方都在同一个 DLL（内部调用），替换整个 DLL 无法改变内部解析，必须用 hooking（IAT/修补/MinHook/Detours）或在源码层注入/抽象（更稳妥）。"  
理解和疑问：  
要 mock 的函数也就是被调函数和调用方都在同一个 DLL 内部，这个 DLL 是集合所有模块静态库的动态库，所以不能加载同名的 mock DLL。请讲解一下 hooking 的原理和方法以及工具，目前环境就是 vs 2013 编译的结果，你提到的 IAT, 修补，MinHook，Detours 的要求是什么？原理是什么？需要什么工具和代码设计？

我要在一个公司的离线 windows 环境下使用 googletest+gmock 来测试一个模块的源代码，这个模块是我们组在维护，在程序运行在 modem 上时表现为一个 threadx 的 task，在 windows 上模拟 threadx 也表现为一个 task，在 visual studio 2013 的 solution 下是一个 project，在资源管理器中表现为一个叫 sapp 的文件夹，源代码不可修改，是 c 98 风格的 c 语言程序，一些 os 的基础函数是另一个平台组叫 sdi 提供的，此外对其他模块还有一些 queue 的交互，此外还存在大量 c 语言复合类型的外部依赖。当前，我已经跑通了一个 googletest+gmock 示例，但是是把源代码拷贝测试的环境下，测试的环境是 windows+mingw gcc 或者 vs 2022+vscode+testmat+google/gmock，测试的文件结构为 build, source, tests, third_party，tests 下放置测试用例，third_party 放置 google+gmock，source 目录是为了尽快跑通所以围绕着要测试的函数将依赖的头文件手动迁移到这儿并替换掉外部依赖，而被测函数则是手动复制到 tests 下的. Cpp 文件中，目前一个. Cpp 文件用于测试一个函数，用于放置 mock 和测试用例。

但问题：
1）源代码无法改动，测试目录可以放置在模块文件夹同级目录
2）手动复制被测试函数到 tests 下的 cpp 文件中在合作维护上不具备可行性
3）直接编译源代码会有巨量的外部依赖缺失
4）一个不成熟的想法是：替换外部依赖、当源码改动时通过脚本等方式自动复制到测试目录，且测试目录下的源代码文件夹的结构和实际源代码文件夹结构一致。
5）当前其他同事疑虑的是测试代码是否和源码一致，特别是我目前采用复制被测函数到 cpp 文件中。
5）之前咨询过你：你提过几种方式，比如编译源码为 lib 然后测试程序链接这个 lib；将源代码编译进入测试程序；由于原 windows 上已经将这个 sapp 模块编译成库并有全部 solution 的 dll，你似乎建议 hook，但我对 hook 的概念都还不清楚。


请你根据以上内容，帮忙给出解决方案。当前环境是在我的个人联网的 windows+wsl 2+ubuntu 上。